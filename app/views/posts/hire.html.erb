<div class="content-container">
  <% if @hire %>
      <h2><%= @hire.title %></h2>
      <%= link_to 'Edit Text', edit_post_path(@hire.id), class: 'btn btn-success' %>
      <p><%= markdown(@hire.body) %></p>
  <% else %>
      <p>Coming Soon!</p>
  <% end %>
</div>
<div class="skew"></div>
