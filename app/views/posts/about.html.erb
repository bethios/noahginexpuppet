<div class="content-container">
  <% if @about %>
      <h2><%= @about.title %></h2>
      <%= link_to 'Edit Text', edit_post_path(@about.id), class: 'btn btn-success' %>
      <p><%= markdown(@about.body) %></p>
  <% else %>
      <p>Coming Soon!</p>
  <% end %>
</div>

